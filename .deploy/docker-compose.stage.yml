version: '2'
services:
    service-api-stage:
        restart: always
        build:            
            args: 
                - ENV_FILE=.deploy/.envs/stage.env
            context: ..
            dockerfile: .deploy/Dockerfile
        ports:
            - "5000:5000"
        volumes:
            - ..:/app
