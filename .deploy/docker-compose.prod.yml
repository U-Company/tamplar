version: '2'
services:
    service-api-prod:
        restart: always
        build:
            args: 
                - ENV_FILE=.deploy/.envs/prod.env
            context: ..
            dockerfile: .deploy/Dockerfile
        ports:
            - "5001:5001"
        volumes:
            - ..:/app
