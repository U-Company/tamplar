version: '3'
services:
    repo_name-external_image_name:
        image: "mongo:4.0.2"
        ports:
            - "27017:27017"
    repo_name:
        args:
            - VAULT_ENV=<--ENVIRONMENT-->
        build:
            context: ..
            dockerfile: .deploy/Dockerfile
        depends_on:
            - dependency-service
        network_mode: host
